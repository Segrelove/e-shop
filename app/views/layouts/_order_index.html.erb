    <div class="col-md-6 col-lg-4">
      <div class="card">
        <img class="card-img-right" src="<%=asset_path('sofa.jpg')%>" alt="Card image cap">
        <div class="card-body">
          <h5 class="card-title">Commande n°<%=order.id%></h5>
          <p class="card-text"><%=order.properties.length%> propriétés</p>
        </div>
        <ul class="list-group list-group-flush">
             <li class="list-group-item"><%=order.created_at.strftime('%Y/%m/%d à %Hh%M')%></li>
        </ul>
      </div>
    </div>

