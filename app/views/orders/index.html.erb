<%=render 'layouts/navbar'%>

<div class="container" style="margin-top:150px; margin-bottom:100px;">
  <div class="row">
    <% @orders.each do |order| %>
      <%=render 'layouts/order_index', order: order%>
    <% end %>
  </div>
</div>

<%=render 'layouts/footer'%>
